import { DateTime, CalendarSpecifier, ZoneSpecifier, LocaleSpecifier } from '../main';

export function parse(
  date: string,
  format: string,
  opts?: { calendar?: CalendarSpecifier, zone?: ZoneSpecifier, locale?: LocaleSpecifier }
): DateTime {
  return;
}
