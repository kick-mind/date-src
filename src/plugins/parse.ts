import { CalendarSpecifier } from "../common";
import { DateTime } from "../date-time";

export function parse(
  date: string,
  format: string,
  opts?: { calendar?: CalendarSpecifier }
): DateTime {
  return;
}
